info:
  name: instituicao - get by id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/instituicao/{{last_created_instituicao_id}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
