info:
  name: get campus
  type: http
  seq: 6

http:
  method: GET
  url: "{{api-url}}/{{target}}/{{uuid}}/campus"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
