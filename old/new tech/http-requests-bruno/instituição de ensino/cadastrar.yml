info:
  name: cadastrar
  type: http
  seq: 1

http:
  method: POST
  url: "{{api-url}}/{{target}}"
  body:
    type: json
    data: |-
      {
        "nome": "Instituto Federal Catarinense 5",
        "sigla": "IFC 5",
        "cnpj": "---5",
        "endereco": "---5",
        "email": "---5",
        "telefone": "---5"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
