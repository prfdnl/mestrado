info:
  name: get by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/publicacao/{{last_created_publicacao_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
