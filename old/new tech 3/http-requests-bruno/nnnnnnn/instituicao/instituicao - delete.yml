info:
  name: instituicao - delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/instituicao/{{last_created_instituicao_id}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
