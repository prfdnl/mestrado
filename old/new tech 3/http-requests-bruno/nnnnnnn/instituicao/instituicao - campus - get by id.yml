info:
  name: instituicao - campus - get by id
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/campus/{{last_created_campus_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
