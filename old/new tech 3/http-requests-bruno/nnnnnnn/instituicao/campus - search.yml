info:
  name: campus - search
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/campus/search/if"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
