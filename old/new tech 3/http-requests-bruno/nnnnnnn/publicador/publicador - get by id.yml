info:
  name: publicador - get by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/publicador/{{user-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
