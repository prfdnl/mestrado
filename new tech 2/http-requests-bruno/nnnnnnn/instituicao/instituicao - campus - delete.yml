info:
  name: instituicao - campus - delete
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{url}}/2/instituicao/{{last_created_instituicao_id}}/campus/{{last_created_campus_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
