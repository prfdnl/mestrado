info:
  name: instituicao - search
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/2/instituicao/search/if"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
