info:
  name: search
  type: http
  seq: 5

http:
  method: GET
  url: "{{api-url}}/{{target}}/search/fed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
