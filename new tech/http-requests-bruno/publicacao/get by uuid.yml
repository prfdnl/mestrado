info:
  name: get by uuid
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-url}}/{{target}}/{{uuid}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
