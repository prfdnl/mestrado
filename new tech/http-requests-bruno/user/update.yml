info:
  name: update
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{api-url}}/user/{{user-id}}"
  body:
    type: json
    data: |-
      {
        "login": "admin 3",
        "password": "admin", 
        "active": false
      }
  auth: inherit

runtime:
  variables:
    - name: userId
      value: qweqew

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
