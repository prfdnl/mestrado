info:
  name: get by uuid
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-url}}/user/{{user-id}}"
  auth: inherit

runtime:
  variables:
    - name: userId
      value: qweqew

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
