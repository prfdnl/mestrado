info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{api-url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "login": "admin",
        "password": "admin"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const body = res.getBody();
        bru.setEnvVar("token", body);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
