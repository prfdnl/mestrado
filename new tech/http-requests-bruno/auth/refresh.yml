info:
  name: refresh
  type: http
  seq: 2

http:
  method: POST
  url: "{{api-url}}/auth/refresh"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const body = res.getBody();
        bru.setEnvVar("token", body);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
