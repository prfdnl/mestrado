info:
  name: atualizar
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{api-url}}/{{target}}/{{uuid}}"
  body:
    type: json
    data: |-
      {
        "nome": "teste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
