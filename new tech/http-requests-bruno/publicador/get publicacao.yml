info:
  name: get publicacao
  type: http
  seq: 6

http:
  method: GET
  url: "{{api-url}}/{{target}}/{{uuid}}/publicacoes"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
