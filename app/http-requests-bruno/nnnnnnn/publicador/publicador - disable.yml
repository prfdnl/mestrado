info:
  name: publicador - disable
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/publicador/{{user-id}}/disable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
