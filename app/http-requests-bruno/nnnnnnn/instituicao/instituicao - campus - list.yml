info:
  name: instituicao - campus - list
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/instituicao/{{last_created_instituicao_id}}/campus"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
