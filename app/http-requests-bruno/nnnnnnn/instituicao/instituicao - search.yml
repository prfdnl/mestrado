info:
  name: instituicao - search
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/instituicao/search/if"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
