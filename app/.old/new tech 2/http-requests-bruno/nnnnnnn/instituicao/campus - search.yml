info:
  name: campus - search
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/2/campus/search/if"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
