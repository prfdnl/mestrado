info:
  name: instituicao - campus - list
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/2/instituicao/{{last_created_instituicao_id}}/campus"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
