info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{api-url}}/user"
  body:
    type: json
    data: |-
      {
        "username": "admin 126",
        "password": "admin"
      }
  auth: inherit

runtime:
  variables:
    - name: userId
      value: qweqew

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
