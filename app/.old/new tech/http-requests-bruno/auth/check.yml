info:
  name: check
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-url}}/auth/check"
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
