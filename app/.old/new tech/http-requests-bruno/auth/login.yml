info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{api-url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
