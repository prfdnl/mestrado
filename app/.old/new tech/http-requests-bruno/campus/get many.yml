info:
  name: get many
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-url}}/{{target}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
